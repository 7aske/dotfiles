#compdef toggle-service

# Complete systemd user units
_toggle-service() {
  local -a services
  services=(${(f)"$(systemctl --user list-units --all --full --no-legend --no-pager --plain | awk '{print $1}')"})
  _describe 'service' services
}

_toggle-service
