#compdef screenlayout

local layout_dir="$HOME/.screenlayout"

if [[ -d $layout_dir ]]; then
  _arguments \
    "1:screen layout:($(
      for f in $layout_dir/*.sh(.N); do
        print -r -- ${f:t}
      done
    ))"
else
  _arguments "1:screen layout:"
fi
